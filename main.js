/**
 * SnapAsk - Main Entry Point
 * This is a thin wrapper that loads the refactored main process
 * 
 * The old code is backed up in main.js.backup
 * The new modular code is in src/main/
 */

// Load the new modular main process
require('./src/main/index');
